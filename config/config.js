// config.js
'use strict';

// setup configuration parameters for mongo database
module.exports = {
    server: {
        api: {
            host: 'localhost',
            port: 4000
        }
    },
    mongo: {
        db: 'calculator'
    }
}
